{
  "mcpServers": {
    "quality-gate-pipeline": {
      "command": "npx",
      "args": [
        "--yes",
        "--prefix",
        "${CLAUDE_PLUGIN_ROOT}",
        "tsx",
        "${CLAUDE_PLUGIN_ROOT}/server.ts"
      ],
      "cwd": "${CLAUDE_PLUGIN_ROOT}"
    }
  }
}
